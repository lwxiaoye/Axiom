> 历史文档，记录 2026-09-17 时的状态；其中工作流/子智能体/皮肤相关内容已于 2026-09-19 移除，正文不再更新。

# 脱敏范围与验证结果

检查日期：2026-09-17。结论针对 Git 可见文件，不包含被忽略的原始备份、`node_modules`、虚拟环境和运行数据。

## 已处理

| 项 | 处理 |
|---|---|
| 产品名 / 包名 | 前端 `axiom-campus-agent`，作者 AXIOM Team，远程 `https://github.com/lwxiaoye/Axiom.git` |
| 皮肤包与开放 API 前缀 | 导出扩展名为 `.axiomskin`；开放 API Key 以 `axa_` 开头，嵌入 Key 以 `axe_` 开头 |
| 上游品牌与学校名称 | 源码与配置中的旧产品名、学校名、旧域名改为 AXIOM / 示例大学 / `example.edu.cn` |
| 登录页与公开文案 | 校园智能体品牌，不再使用上游登录装饰文案 |
| 部署地址 | 旧内网地址改为 `127.0.0.1` 或占位符 |
| 凭据 | 真实 `.env`、证书、私钥未纳入发布文件；模板使用 `CHANGE_ME` |
| 运行数据 | 用户文件、向量库、数据库、日志、截图不入库 |
| 原始工作区 | 嵌套的原始源码目录与 `__MACOSX/` 仅本地备份，已被 `.gitignore` 排除 |
| 第三方统计 | 已移除原页面中的百度统计脚本 |
| 独立 Git 历史 | 不导入上游 `.git`，避免旧提交里的密钥随历史泄露 |

## 有意保留

- `LICENSE`、`NOTICE.md` 及依赖版权声明，用于说明来源，不是继续使用旧产品名。
- Java 接口路径、表名、Jeecg 组件命名空间：它们是兼容契约，改掉会无法对接业务后端。
- 测试夹具中的合成 `sk-` 字符串：不是真实密钥。

## 发布检查

```powershell
python scripts/check_release.py
```

本轮扫描结果：`files_checked` 覆盖 Git 可见文件，`passed=true`，无品牌、密钥、证书或旧部署 IP 命中。

## 不要这样做

- 不要打包整个 `D:\lwxiaoye\Axiom` 目录；其中含原始备份和依赖。
- 不要提交 `agent-api/.env`、`deploy/local/.env`、证书或用户数据。
- 不要把聊天里的 GitHub 令牌写入远程 URL 或任何入库文件。
